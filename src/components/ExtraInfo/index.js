import React from 'react';

import { View, Text } from 'react-native';

import styles from './styles';

const ExtraInfo = ({ data }) => (
  <View style={styles.container}>
    <Text style={styles.city}>teste</Text>
  </View>
);

export default ExtraInfo;
